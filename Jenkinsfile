#!/usr/bin/env groovy

def lvProjectPath = "Resizable Alert Dialog.lvproj"
def lvBuildSpecName = "myBuildSpec"
def lvVersion = "21.0"
def lvBitness = "64"

lvPipeline(lvProjectPath, lvBuildSpecName, lvVersion, lvBitness)